"use strict";

class ClassName
{
	// class scope
	constructor (attribute1, ..., attributeN) {}
}

// To instantiate a object is just needed to create a new variable and the attribute to the variable
// the object using the reserved word "new" followed by the class name
const object = new ClassName(attribute1, ..., attributeN);
